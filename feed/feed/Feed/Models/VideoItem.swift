
import Foundation

class VideoItem {
    let imageUrl: String
    let playerUrl: String
    
    init(imageUrl: String, playerUrl: String) {
        self.imageUrl = imageUrl
        self.playerUrl = playerUrl
    }
}
